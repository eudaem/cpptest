#include <iostream> 
#include <string> 
 
using namespace std; 
 
int main() 
{ 
    string str("a simple string");
    for(unsigned int i = 0; i < str.size(); i++) 
    { 
        str[i] = 'X'; 
    } 
    cout << str <<endl; 
 
  /*int i = 0;
    while(i < str.size())
    {
        str[i] = 'X';
        ++i;
    }
    cout << str << endl;
   */
   
    return 0; 
} 
